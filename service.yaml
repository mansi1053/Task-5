---
apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx-image
spec:
  selector:
    app: nginx-image
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007